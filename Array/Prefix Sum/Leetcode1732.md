<!-- 
1732. Find the Highest Altitude

I have used an approach to find the maximum of the array by taking a variable max which store the maximum value for the variable and then i have used Math.max to find the maximum value between the 
Integer.MIN_VALUe and the array value
        int max=Integer.MIN_VALUE;
        max=Math.max(i,max);
 it help me to find the maximum value from the array and then return the max value
 -->